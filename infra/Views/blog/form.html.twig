{% extends "base.html.twig" %}

{% block content %}
    <h1>Créer un post</h1>
    {% include "parts/form.error.html.twig" with {'key': 'global'} %}

    <form action="" method="post">
        <div class="form-group">
            <label for="title">Titre</label>
            <input type="text" name="title" id="title" class="form-control">
            {% include "parts/form.error.html.twig" with {'key': 'title'} %}
        </div>
        <div class="form-group">
            <label for="content">Contenu</label>
            <textarea name="content" id="content" class="form-control"></textarea>
            {% include "parts/form.error.html.twig" with {'key': 'content'} %}
        </div>
        <button class="btn btn-primary my-2">Enregistrer</button>
    </form>
{% endblock %}